export const AUTO_ROTATE_SPEED = 0.5;
export const AUTO_ROTATE_MAX_DISTANCE = 14;
export const AUTO_ROTATE_SIZE_DISTANCE_FACTOR = 3;

// Models listed here spin continuously when editor mode is OFF.
// Axis must be one of: x, y, z.
export const AUTO_ROTATE_MODEL_AXES = {
  skill_architecture: "z",
  skill_network: "y",
  skill_math: "y",
};
